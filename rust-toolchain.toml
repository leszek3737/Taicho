[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
# Always the latest stable Rust. This is an application, not a published library, so it
# has no MSRV contract — `cargo` and CI track current stable (edition 2024).
